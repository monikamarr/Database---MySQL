
{{!-- Create customer table --}}
<h2>ℙ𝕣𝕠𝕕𝕦𝕔𝕥𝕤</h2>
<img src="/img/im.jpeg" alt="Our propane product">
<table id="products-table">

    {{!-- Header section --}}
    <thead>

        {{!-- For just the first row, we print each key of the row object as a header cell so we
        know what each column means when the page renders --}}
        <tr>
            {{#each data.[0]}}
            <th>
                {{@key}}
            </th>
            {{/each}}
        </tr>
    </thead>

    {{!-- Body section --}}
    <tbody>

        {{!-- For each row, print the id, fname, lname, homeworld and age, in order --}}
        {{#each data}}
        <tr>
            <td>{{this.idProduct}}</td>
            <td>{{this.productName}}</td>
            <td>{{this.wholesalePrice}}</td>
            <td>{{this.retailPrice}}</td>
            <td>{{this.idVendor}}</td>
           
        </tr>
        {{/each}}
    </tbody>
</table>

<!-- /views/index.hbs -->

{{!-- Form to add a record to the table  --}}


<h2>Add New Product</h2>
<p>To add a new product, please enter their information below and click 'Submit'!</p>
<form id="add-product-form-ajax">
    <label for="input-productName">Product Name: </label>
    <input type="text" name="input-productName" id="input-productName">
    
    <label for="input-wholesalePrice">Wholesale Price: </label>
    <input type="text" name="input-wholesalePrice" id="input-wholesalePrice" placeholder="$">

    <label for="input-retailPrice">Retail Price: </label>
    <input type="text" name="input-retailPrice" id="input-retailPrice" placeholder="$">


<select name="input-vendorName" id="mySelect">
        <option value="test">Select Vendor</option>
        {{#each data}}
        <option value="{{this.idVendor}}">{{this.idVendor}}</option>
        {{/each}}
    </select>


    <input type="submit">
</form>

{{!-- Embed our javascript to handle the DOM manipulation and AJAX request --}}
<script src=".js/add_product.js"></script>   {{!--CHANGED FROM MONIKA'S APP--}}


{{!--SEARCH PRODUCTS table --}}

<h2>Search For a Product</h2>
<p>Search by product name using the field below. Partial product names are OK too!</p>
<form id="search-product-html" method='GET' action='/products'>
    <label for="input-pname">Search by Product Name: </label>
    <input type="text" name="pname" id="input-pname-html-search">
    
    <input type="submit">
    <a href='/'><button>Reset</button></a>
</form>

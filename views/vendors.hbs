
{{!-- Create vendor table --}}
<h2>𝕍𝕖𝕟𝕕𝕠𝕣𝕤</h2>
<table id="vendors-table">

    {{!-- Header section --}}
    <thead>

        {{!-- For just the first row, we print each key of the row object as a header cell so we
        know what each column means when the page renders --}}
        <tr>
            {{#each data.[0]}}
            <th>
                {{@key}}
            </th>
            {{/each}}
        </tr>
    </thead>

    {{!-- Body section --}}
    <tbody>

        {{!-- For each row, print the id, fname, lname, homeworld and age, in order --}}
        {{#each data}}
        <tr>
            <td>{{this.idVendor}}</td>
            <td>{{this.vendorName}}</td>
            <td>{{this.vendorAddress}}</td>
           
        </tr>
        {{/each}}
    </tbody>
</table>
<div class="container">
{{!-- INSERT --}}
<div class="add-vendor">
{{!-- Form to add a record to the table  --}}
<h2>Add Vendor</h2>
<p class="small">To add a new vendor, enter their information below and hit "Submit" </p>
<form id="add-vendor-form-ajax">
    <label for="input-vendorName">Vendor's Name: </label>
    <input type="text" name="input-vendorName" id="input-vendorName">
    
    <label for="input-vendorAddress">Vendor's Address: </label>
    <input type="text" name="input-vendorAddress" id="input-vendorAddress" placeholder="Street City State Zip Code">

    <input type="submit">
</form>

{{!-- Embed our javascript to handle the DOM manipulation and AJAX request --}}
<script src=".js/add_vendor.js"></script>   {{!--CHANGED FROM MONIKA'S APP--}}
</div>
<div class="update-vendor">
{{! --UPDATE --}}
<h2>Update Vendor</h2>
<p class="small">To update a specific vendor, select their name from the drop-down menu</p>
<form id="update-vendor-form-ajax">

    <label for="input-vendorName">Vendor's Name: </label>
    <select name="input-vendorName" id="mySelect">
        <option value="test">Select Vendor</option>
        {{#each data}}
        <option value="{{this.idVendor}}">{{this.vendorName}}</option>
        {{/each}}
    </select>

    <label for="input-address">Address: </label>
<input type="text" name="input-address" id="input-address-update" placeholder="Enter new address">


    <input type="submit"> 

</form>
</div>
</div>
<script src="./js/update_vendor.js"></script>
